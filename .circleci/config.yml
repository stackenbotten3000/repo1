version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run: echo "Build succeeded"
      - run: echo "X = ${X:-no}"

workflows:
  main:
    jobs:
      - build
