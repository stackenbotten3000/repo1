version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: "Build test"
          command: "echo Build succeeded"
      - run:
          name: "Check secrets"
          command: "echo X = ${X:-no}"

workflows:
  main:
    jobs:
      - build
